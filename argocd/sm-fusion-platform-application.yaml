# ArgoCD Application: sm-fusion-platform（GitOps 持续交付）
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: sm-fusion-platform
  namespace: argocd
  labels:
    app.kubernetes.io/name: sm-fusion-platform
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: sm-platform
  source:
    repoURL: https://github.com/luoshitianchen/sm-fusion-platform.git
    targetRevision: HEAD
    path: helm/sm-fusion-platform
    helm:
      valueFiles:
        - values.yaml
        - values-prod.yaml
  destination:
    server: https://kubernetes.default.svc
    namespace: sm-prod
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
      allowEmpty: false
    syncOptions:
      - CreateNamespace=true
      - ApplyOutOfSyncOnly=true
    retry:
      limit: 5
      backoff:
        duration: 5s
        factor: 2
        maxDuration: 3m
